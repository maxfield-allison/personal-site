---
// Inline SVG icon system. Zero client JS: brand marks come from the MIT
// simple-icons package at build time; a few generic glyphs (email, cloud) are
// hand-authored Lucide-style strokes. simple-icons dropped LinkedIn and the
// Microsoft/Azure marks for legal reasons, so those are provided locally.
//
// Usage: <Icon name="github" /> or <Icon name="kubernetes" size={14} />
// Unknown names render nothing (callers degrade to the text label).
import {
  siGithub,
  siBluesky,
  siRss,
  siKubernetes,
  siGo,
  siDocker,
  siTerraform,
  siAnsible,
  siLinux,
  siArgo,
  siGitlab,
  siCloudflare,
  siOpentofu,
  siGnubash,
  siHtml5,
} from 'simple-icons';

interface Props {
  name: string;
  size?: number;
  class?: string;
}

const { name, size = 16, class: className } = Astro.props;

// Filled single-path brand icons (24x24 viewBox), fill=currentColor.
const filled: Record<string, string> = {
  github: siGithub.path,
  bluesky: siBluesky.path,
  rss: siRss.path,
  kubernetes: siKubernetes.path,
  go: siGo.path,
  docker: siDocker.path,
  terraform: siTerraform.path,
  ansible: siAnsible.path,
  linux: siLinux.path,
  argo: siArgo.path,
  gitlab: siGitlab.path,
  cloudflare: siCloudflare.path,
  opentofu: siOpentofu.path,
  bash: siGnubash.path,
  html: siHtml5.path,
  // Canonical LinkedIn "in" mark (simple-icons removed it for legal reasons).
  linkedin:
    'M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065 2.064 2.064 0 1 1 2.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.225 0z',
};

// Stroke glyphs (Lucide-style, 24x24, stroke=currentColor) for generic icons
// that have no clean brand mark.
const stroke: Record<string, string> = {
  email: '<rect x="2" y="4" width="20" height="16" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>',
  azure: '<path d="M17.5 19H9a7 7 0 1 1 6.71-9h1.79a4.5 4.5 0 1 1 0 9Z"/>',
};
---

{
  filled[name] && (
    <svg
      role="img"
      aria-hidden="true"
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="currentColor"
      class={className}
    >
      <path d={filled[name]} />
    </svg>
  )
}
{
  stroke[name] && (
    <svg
      role="img"
      aria-hidden="true"
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="2"
      stroke-linecap="round"
      stroke-linejoin="round"
      class={className}
      set:html={stroke[name]}
    />
  )
}
